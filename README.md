# UI Project

## Quick Start

```bash
cd ui
./bootstrap.sh
```

By default the UI points to `http://localhost:8000`. Override if needed:

```bash
VITE_API_BASE_URL=http://your-api:8000 ./bootstrap.sh
```

## Health Check

```bash
./healthcheck.sh
```
